# Data Directory

This directory stores baseline calibration data and user profiles:

- Baseline neutral expression data
- Calibration parameters
- User-specific settings

Data is saved in JSON format for easy integration.
